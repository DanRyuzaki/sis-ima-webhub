class registrarAnalyticsModel {
  int totalClasses;
  int totalSubjects;
  int totalStudents;
  int totalTeachers;

  registrarAnalyticsModel({
    required this.totalClasses,
    required this.totalSubjects,
    required this.totalStudents,
    required this.totalTeachers,
  });
}
